# Notion integration token. Create it in Notion integrations, then share only
# the selected source pages/databases with that integration.
NOTION_TOKEN=secret_xxx

# Optional: inline JSON config for Vercel. If omitted, the script reads
# notion-sync.config.cjs from the repository.
# NOTION_SYNC_CONFIG={"mappings":[]}
